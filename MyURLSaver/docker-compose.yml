version: '3'

services:
  db:
    image: postgres
  web:
    build: .
    command: python3 manage.py runserver 0.0.0.0:8000
    volumes:
      - .:/code
    ports:
      - "8000:8000"
    environment:
      - MyURLSaverProdDEBUG=0
      - MyURLSaverProdKey=1111111
    depends_on:
      - db
